# Admin Pages
- These are admin pages that only need to be plugged in to the backend APIs / methods in order to work.
- Due to the recent npm attacks, backend development was halted for now, due to being required to install some packages.