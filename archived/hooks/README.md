# Backend hooks
- UseAuth is for connecting the user/auth api
- useGenerators is for connecting the generators api
- These are archived for now as these are untested code that will require installation of additional packages/libraries. I'll be installing packages and test these for proper integration in the future.